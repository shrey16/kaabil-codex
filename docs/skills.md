# Skills

Skills are reusable instruction bundles stored under `~/.kaabil-codex/skills` (or `CODEX_HOME/skills`).
Each skill is defined by a `SKILL.md` file and can include supporting files or scripts.

When a skill is referenced by name, the agent loads its `SKILL.md` and follows its workflow.
