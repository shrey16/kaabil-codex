# Authentication

Kaabil Codex supports two common flows:

- `codex auth login` to start the interactive sign-in flow.
- Set `OPENAI_API_KEY` (or a provider-specific key) to use API-key auth.

Auth settings live in `~/.kaabil-codex` (or the directory set by `CODEX_HOME`).
